<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name = "MyStyleTab" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorAccent">@color/colorWhite</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>


    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="orange_label">
        <item name="android:paddingTop">@dimen/label_vertical_padding</item>
        <item name="android:paddingBottom">@dimen/label_vertical_padding</item>
        <item name="android:paddingLeft">@dimen/label_horizontal_padding</item>
        <item name="android:paddingRight">@dimen/label_horizontal_padding</item>
        <item name="android:background">@drawable/label_background</item>
        <item name="android:textColor">#EEEEEE</item>
        <item name="android:textSize">@dimen/label_text_size</item>
        <item name="android:layout_marginTop">@dimen/margin_top</item>
    </style>
    <style name="main_text_left">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">@dimen/text_size</item>
        <item name="android:textColor">@color/colorGray</item>
    </style>
    <style name="main_text_right">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">@dimen/text_size</item>
        <item name="android:textColor">@color/colorBlack</item>
        <item name="android:layout_alignParentRight">true</item>
    </style>

    <style name="text_date_cardView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">@dimen/txt_textSize</item>
    </style>
</resources>
